# OmaCalendar 1.1.1

A correctness patch for all-day events whose upstream copy uses an inclusive
end date. No interface, schema, or feature change.

## Candidate identity

App 1.1.1; IPC 2.1; schema 2 (additive in-place schema repairs only); current
x86-64 Arch/Omarchy. The companion widget is independently released. Runtime
source: `1981fe5` (merged PR #29). Release run
[35750807263](https://github.com/brdweb/omacalendar/actions/runs/35750807263)
built, attested and drafted the `v1.1.1` assets; the owner published that draft
separately on 2026-09-22. Released package SHA-256:
`f738c482dd45189810d0d06419523bee4acab7aa851653440780a82d46ab999c`.

The owner fast-tracked this patch on 2026-09-22. Gates marked `WAIVED` below
were consciously skipped, not run: no Clang or sanitizer matrix, no release
performance harness, no live-provider smoke, no upgrade rehearsal, and no owner
acceptance on the release-reference system. The evidence column states exactly
what did run for each waived gate. See [RELEASE.md](../RELEASE.md) for the
waiver rule.

The cited candidate CI runs cover commit `ba4a0ee`; the documentation commits
after it changed only this acceptance record and re-ran the same checks. The
release itself was built from `1981fe5`, which main's own CI also passed.

## Fixed behavior

- Some publishers, including subscribed Google calendars, describe a one-day
  all-day event with an inclusive end date (`end.date`/`DTEND` equal to the
  start date). Both provider read paths now normalize such a span to the
  exclusive next-day end that the rest of the app assumes.
- An iCalendar payload containing an all-day event that ends before it starts
  is repaired to a one-day event instead of failing to parse as a whole, so one
  malformed `VEVENT` can no longer discard an entire subscription.
- An inclusive span never reaches storage, whichever caller supplies it, and a
  cache that already holds one is repaired when the daemon next opens it. Range
  queries compare the stored end date in SQL, so those events would otherwise
  stay invisible until a resync.

## Interface and data

No IPC, schema, packaging, or configuration change. `docs/IPC.md` is unchanged.

## Pre-tag gates

| Gate | Status | Evidence |
|---|---|---|
| Exact app version metadata and prerelease tooling | PASS | CI "Source and package metadata" [35741273796](https://github.com/brdweb/omacalendar/actions/runs/35741273796) validated formatting, shell scripts, Arch assembly, SBOM finalization, and desktop/AppStream metadata; `packaging/release/test-versioning.sh` also passes locally on this tree. |
| Clean GCC and Clang builds with full automated matrix | WAIVED - owner fast-track on 2026-09-22 | The GCC Release/Werror baseline built and passed all 23 ctest suites, daemon and desktop smoke, and qmllint in run [35741273796](https://github.com/brdweb/omacalendar/actions/runs/35741273796). The Clang and extended matrix jobs were skipped and were not run for this candidate. |
| ASan/UBSan matrix, including IPC framing/reconnect regressions | WAIVED - owner fast-track on 2026-09-22 | No sanitizer build ran for this candidate. The IPC and daemon contract suites passed unsanitized in the baseline run. |
| 100,000-event release performance harness without daemon crash | WAIVED - owner fast-track on 2026-09-22 | Only the `release_performance_smoke` suite ran, inside the baseline ctest run. The 100,000-event harness was not executed. |
| Staged `/usr` install, desktop launch, daemon restart, and uninstall | PASS | Baseline run [35741273796](https://github.com/brdweb/omacalendar/actions/runs/35741273796) staged a `/usr` install, passed `verify-install.sh`, `smoke-daemon.sh` restart and `smoke-app.sh` offscreen startup, and passed `verify-uninstall.sh` against the install manifest. |
| Local CRUD/offline cache, Radicale round trip, and public ICS refresh | WAIVED - owner fast-track on 2026-09-22 | No live-provider smoke ran. The automated local, CalDAV, ICS and sync-coordinator suites passed, including this release's new all-day parse coverage. |
| Secret scan and dependency review | PASS | Gitleaks [35741274106](https://github.com/brdweb/omacalendar/actions/runs/35741274106) and dependency review [35741273879](https://github.com/brdweb/omacalendar/actions/runs/35741273879) passed on the candidate branch. The change adds no dependency. |
| No unresolved critical or high defect | PASS | Issue #28 was the only open issue and is fixed here with regression coverage. CodeQL passed on the candidate branch; GitHub reports no open code-scanning alerts. |
| Current-Omarchy owner acceptance on the exact runtime | WAIVED - owner fast-track on 2026-09-22 | No owner run on the release-reference Omarchy system was performed for this candidate. |
| Prior-release upgrade, backup/restore, and uninstall | WAIVED - owner fast-track on 2026-09-22 | Clean uninstall passed in the baseline run. No 1.1.0-to-1.1.1 upgrade or backup/restore rehearsal was performed. |
| Privacy-safe screenshots and public release documentation | PASS | Changelog, AppStream, compatibility, installation and download references were updated for 1.1.1 and reviewed. The existing synthetic screenshots are unaffected by this change. |
| Arch package passes clean validation | PASS | Release run [35750807263](https://github.com/brdweb/omacalendar/actions/runs/35750807263) built the native Arch package and installed, exercised and removed it in a clean `archlinux:base` container, covering owned documentation, library resolution, daemon restart and desktop scaling. |

## Post-tag draft gates

| Gate | Status | Evidence |
|---|---|---|
| Signed app tag and exact target commit independently verified | PASS | `v1.1.1` is an annotated tag SSH-signed by the release key and points at `1981fe5`, the merge commit of PR #29. GitHub verified the tag signature inside release run [35750807263](https://github.com/brdweb/omacalendar/actions/runs/35750807263), and `packaging/release/verify-release.sh v1.1.1` passed locally against the clean checkout. |
| Checksums and package-specific provenance/SBOM attestations verified | PASS | Every published asset matched `SHA256SUMS` on a fresh download. `gh attestation verify` confirmed SLSA provenance and the SPDX v2.3 predicate for the Arch package, and provenance for the source archive, each bound to `refs/tags/v1.1.1` and the release workflow. Released package SHA-256: `f738c482dd45189810d0d06419523bee4acab7aa851653440780a82d46ab999c`. |
| Native Arch package installation, restart, and removal | PASS | The tagged package was installed, exercised and removed in a clean `archlinux:base` container in run [35750807263](https://github.com/brdweb/omacalendar/actions/runs/35750807263), including a daemon restart smoke test under `dbus-run-session`. It has not been installed on the release-reference Omarchy system. |
| Complete owner and live-provider acceptance | WAIVED - owner fast-track on 2026-09-22 | No live-provider acceptance was run against the released package. |
| Maintainer approval to publish | APPROVED | The owner directed fast-track publication of this patch on 2026-09-22, accepting the waived gates recorded above, and published the workflow's draft as the latest release the same day. |

## Known limitations

- A malformed all-day span is repaired silently rather than reported: an
  upstream event whose end date precedes its start date becomes a one-day
  event, and the original span is not surfaced to the user.
- All limitations from [1.1.0](1.1.0.md) continue to apply.

## External approvals

| Approval | Status | Evidence |
|---|---|---|
| Historical Google installed-app OAuth credential revoked or rotated | APPROVED | Prior incident resolution is unchanged; credential handling is untouched by this release. |
| Repository-history hygiene decision recorded | APPROVED | Continue the accepted 1.0.0 policy: preserve signed history and keep revoked credentials revoked. |
| App repository committed, clean, pushed, and green in clean-checkout CI | APPROVED | Candidate branch and main checks passed: metadata and Arch baseline [35741273796](https://github.com/brdweb/omacalendar/actions/runs/35741273796), Gitleaks, dependency review and CodeQL. The extended matrix was skipped and is waived above. |
| Maintainer approval to create the signed app tag | APPROVED | The owner directed fast-tracking this patch to a tagged release on 2026-09-22. |
| Google OAuth branding and sensitive-scope verification approved | APPROVED | Requested scopes and the Google auth implementation are unchanged from 1.1.0. |
| Maintainer approval of public release acceptance and limitations | APPROVED | The owner accepted the waived gates and the known limitations below on 2026-09-22. |
