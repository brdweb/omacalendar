# OmaCalendar 1.1.0

A notification-hardening and desktop polish release. It focuses on how imported
calendar history and invitation alerts behave: adding a calendar with old
reminders or past invitations no longer floods the notification center, and
invitation state survives daemon restarts.

## Candidate identity

App 1.1.0; IPC 2.1; schema 2 (additive in-place schema repairs only); current
x86-64 Arch/Omarchy. The companion widget is independently released. The exact
source commit, signed tag, package hash, and workflow runs remain pending until
the release candidate is built and qualified.

## Notification behavior

- Reminders whose trigger time is more than two hours past and whose occurrence
  already started are dismissed silently. Reminders for still-upcoming
  occurrences deliver as catch-up when the daemon returns.
- Per-calendar "Mute alerts" silences both reminders and invitation
  notifications for that calendar.
- Invitation notifications cover recent invitations only; invitation history
  older than 48 hours past its occurrence end is imported silently. This also
  covers the yearly history backfill after adding a new calendar.
- Invitation baselines are durable: changes that arrive while the daemon is
  offline notify after restart instead of being swallowed.
- More than five simultaneous invitations collapse into a single digest
  notification.

## Desktop UI

- Inline connecting/queued/connected feedback on the Google, CalDAV and ICS
  account forms and a "Mute alerts" option for new local calendars.
- A cached-data sync indicator, actionable empty states, keyboard support for
  date cells, calendar reordering and the month overflow popup, theme tokens
  for spacing and radii, unified date/time formatting, responsive window
  minimums, and consistent glyphs with subtle transitions.

## Interface and data

- `invitations.list` adds additive `upcomingTotal`/`pastTotal` fields; see
  [IPC](../IPC.md). No schema version change: the release ships only the
  established in-place schema repair path.

## Pre-tag gates

| Gate | Status | Evidence |
|---|---|---|
| Exact app version metadata and prerelease tooling | PENDING | Run the release metadata and versioning gates on the exact clean candidate. |
| Clean GCC and Clang builds with full automated matrix | PENDING | Exact-candidate clean-checkout CI has not run. |
| ASan/UBSan matrix, including IPC framing/reconnect regressions | PENDING | Exact-candidate sanitizer CI has not run. |
| 100,000-event release performance harness without daemon crash | PENDING | Exact-candidate performance evidence has not been recorded. |
| Staged `/usr` install, desktop launch, daemon restart, and uninstall | PENDING | Exact-candidate staged lifecycle evidence has not been recorded. |
| Local CRUD/offline cache, Radicale round trip, and public ICS refresh | PENDING | Exact-candidate provider and offline acceptance has not been run. |
| Secret scan and dependency review | PENDING | Exact-candidate security workflow evidence has not been recorded. |
| No unresolved critical or high defect | PENDING | Final defect review and maintainer disposition are required. |
| Current-Omarchy owner acceptance on the exact runtime | PENDING | Owner acceptance on the exact package has not been performed. |
| Prior-release upgrade, backup/restore, and uninstall | PENDING | The 1.0.0-to-1.1.0 lifecycle has not been qualified on the exact package. |
| Privacy-safe screenshots and public release documentation | PENDING | Final public materials have not been reviewed against the exact candidate. |
| Arch package passes clean validation | PENDING | The exact Arch package has not been built or validated. |

## Post-tag draft gates

| Gate | Status | Evidence |
|---|---|---|
| Signed app tag and exact target commit independently verified | PENDING | No 1.1.0 release tag has been created. |
| Checksums and package-specific provenance/SBOM attestations verified | PENDING | No tagged 1.1.0 assets or attestations exist to verify. |
| Native Arch package installation, restart, and removal | PENDING | No downloaded tagged package has been exercised. |
| Complete owner and live-provider acceptance | PENDING | Owner and isolated live-provider acceptance have not been performed. |
| Maintainer approval to publish | PENDING | Publication has not been approved. |

## Known limitations

- Event series and individual occurrences can move between writable calendars.
  Cross-account or cross-provider moves require confirmation. Cross-calendar
  this-and-future moves remain unsupported.
- All other limitations from [1.0.0](1.0.0.md) continue to apply.

## External approvals

| Approval | Status | Evidence |
|---|---|---|
| Historical Google installed-app OAuth credential revoked or rotated | PENDING | Confirm the prior incident record remains applicable to the 1.1.0 candidate. |
| Repository-history hygiene decision recorded | PENDING | Record the maintainer's 1.1.0 history-hygiene review. |
| App repository committed, clean, pushed, and green in clean-checkout CI | PENDING | The exact 1.1.0 candidate commit and clean-checkout CI run do not yet exist. |
| Maintainer approval to create the signed app tag | PENDING | No approval to tag 1.1.0 has been recorded. |
| Google OAuth branding and sensitive-scope verification approved | PENDING | Confirm the existing approval remains valid for the unchanged requested scopes. |
| Maintainer approval of public release acceptance and limitations | PENDING | Final acceptance and limitation approval has not been recorded. |
