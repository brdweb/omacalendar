# OmaCalendar 1.1.0

A notification-hardening and desktop polish release. It focuses on how imported
calendar history and invitation alerts behave: adding a calendar with old
reminders or past invitations no longer floods the notification center, and
invitation state survives daemon restarts.

## Candidate identity

App 1.1.0; IPC 2.1; schema 2 (additive in-place schema repairs only); current
x86-64 Arch/Omarchy. The companion widget is independently released. Runtime source: `bfa6c1df1f70ddeef57ca76cc0a0b7853fa2f3e8` (merged PR #26).
The publication commit changes only this acceptance record and the website download link. The signed tag,
final package hash, and attestation verification will be recorded in the GitHub
release after the tag workflow completes.

The [Arch rehearsal](https://github.com/brdweb/omacalendar/actions/runs/35548460492)
passed all 23 tests, startup, staging, package assembly, and clean Arch
installation/removal. Every downloaded asset matched `SHA256SUMS`.
Rehearsal package SHA-256:
`87816f4573dca109a476e551dddeae86086ece01bb9dd86e604f37939456274d`.
These rehearsal bytes are qualification evidence; the signed-tag workflow
builds and attests the final release separately.

## Notification behavior

- Reminders whose trigger time is more than two hours past and whose occurrence
  already started are dismissed silently. Reminders for still-upcoming
  occurrences deliver as catch-up when the daemon returns.
- Per-calendar "Mute alerts" silences both reminders and invitation
  notifications for that calendar.
- Invitation notifications cover recent invitations only; invitation history
  older than 48 hours past its occurrence end is imported silently. This also
  covers the yearly history backfill after adding a new calendar.
- Invitation baselines are durable: changes that arrive while the daemon is
  offline notify after restart instead of being swallowed.
- More than five simultaneous invitations collapse into a single digest
  notification.

## Desktop UI

- Inline connecting/queued/connected feedback on the Google, CalDAV and ICS
  account forms and a "Mute alerts" option for new local calendars.
- A cached-data sync indicator, actionable empty states, keyboard support for
  date cells, calendar reordering and the month overflow popup, theme tokens
  for spacing and radii, unified date/time formatting, responsive window
  minimums, and consistent glyphs with subtle transitions.

## Interface and data

- `invitations.list` adds additive `upcomingTotal`/`pastTotal` fields; see
  [IPC](../IPC.md). No schema version change: the release ships only the
  established in-place schema repair path.

## Pre-tag gates

| Gate | Status | Evidence |
|---|---|---|
| Exact app version metadata and prerelease tooling | PASS | Versioning and metadata gates passed in main CI and the exact-source Arch rehearsal. |
| Clean GCC and Clang builds with full automated matrix | PASS | Extended CI [35550378265](https://github.com/brdweb/omacalendar/actions/runs/35550378265) passed GCC, Clang, and all automated suites on runtime bfa6c1d; publication changes only documentation and the website link. |
| ASan/UBSan matrix, including IPC framing/reconnect regressions | PASS | Extended CI [35550378265](https://github.com/brdweb/omacalendar/actions/runs/35550378265) passed ASan/UBSan on runtime bfa6c1d, including the IPC and daemon contract suites. |
| 100,000-event release performance harness without daemon crash | PASS | The release_performance_smoke test passed in rehearsal run 35548460492; hosted timing is not a reference-hardware benchmark. |
| Staged `/usr` install, desktop launch, daemon restart, and uninstall | PASS | Rehearsal run 35548460492 passed staged lifecycle, daemon restart, and offscreen desktop startup; owner confirmed desktop testing. |
| Local CRUD/offline cache, Radicale round trip, and public ICS refresh | PASS | Downloaded rehearsal package passed live-provider-smoke.py: real disposable Radicale discovery, CRUD readback, offline queue/restarts; authenticated HTTPS ICS refresh, credential reload/rotation and reconnect. Owner separately confirmed hosted-provider acceptance on 2026-09-20. |
| Secret scan and dependency review | PASS | Main secret scan 35547544910 and PR dependency review 35547222113 passed; GitHub reports no open secret-scanning or Dependabot alerts. |
| No unresolved critical or high defect | PASS | GitHub review found no open issues or code-scanning alerts. Both PR #26 findings were fixed in a8c82e0 and bfa6c1d, with reminder regression and owner desktop verification. |
| Current-Omarchy owner acceptance on the exact runtime | PASS | Owner confirmed completed 1.1.0 testing and instructed tag/publication on 2026-09-20. |
| Prior-release upgrade, backup/restore, and uninstall | PASS | Owner confirmed completed 1.1.0 upgrade/backup recovery acceptance on 2026-09-20; clean package removal also passed the rehearsal. |
| Privacy-safe screenshots and public release documentation | PASS | Reviewed supported Arch-only installation, release changes, compatibility, and existing synthetic screenshots; owner accepted the release. |
| Arch package passes clean validation | PASS | Rehearsal run 35548460492 built, validated, installed, exercised, and removed the exact native Arch package; downloaded checksums passed. |

## Post-tag draft gates

| Gate | Status | Evidence |
|---|---|---|
| Signed app tag and exact target commit independently verified | PENDING | No 1.1.0 release tag has been created. |
| Checksums and package-specific provenance/SBOM attestations verified | PENDING | No tagged 1.1.0 assets or attestations exist to verify. |
| Native Arch package installation, restart, and removal | PENDING | No downloaded tagged package has been exercised. |
| Complete owner and live-provider acceptance | PASS | Owner confirmed completed 1.1.0 testing on 2026-09-20; final tagged package automation remains a separate gate. |
| Maintainer approval to publish | APPROVED | Owner explicitly requested that the 1.1.0 tag and release be completed on 2026-09-20. |

## Known limitations

- Event series and individual occurrences can move between writable calendars.
  Cross-account or cross-provider moves require confirmation. Cross-calendar
  this-and-future moves remain unsupported.
- All other limitations from [1.0.0](1.0.0.md) continue to apply.

## External approvals

| Approval | Status | Evidence |
|---|---|---|
| Historical Google installed-app OAuth credential revoked or rotated | APPROVED | Owner reconfirmed existing incident resolution on 2026-09-20; prior release records revocation and credential handling is unchanged. |
| Repository-history hygiene decision recorded | APPROVED | Continue the accepted 1.0.0 policy: preserve signed history and keep revoked credentials revoked; owner reconfirmed existing resolution. |
| App repository committed, clean, pushed, and green in clean-checkout CI | APPROVED | Runtime bfa6c1d passed main CI 35547544991 and rehearsal 35548460492. This evidence-only publication update must pass protected PR checks before tagging. |
| Maintainer approval to create the signed app tag | APPROVED | Owner explicitly requested the 1.1.0 tag on 2026-09-20. |
| Google OAuth branding and sensitive-scope verification approved | APPROVED | Owner reconfirmed existing Google OAuth approval on 2026-09-20; requested scopes and Google auth implementation are unchanged from 1.0.0. |
| Maintainer approval of public release acceptance and limitations | APPROVED | Owner confirmed testing complete and directed 1.1.0 publication on 2026-09-20. |
